`main-window.png` is now copied from `docs/image.png`, which is the current local screenshot for the app.

The AppStream metadata currently points at `https://raw.githubusercontent.com/saleguas/LibrewolfProfiles/main/docs/image.png`. Local validation will continue to warn until that file exists at the GitHub URL, but the image asset in this repository is now aligned with the metadata and ready to publish.
